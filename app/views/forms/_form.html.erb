<%= form_for(form) do |f| %>
  <% if form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(form.errors.count, "error") %> prohibited this form from being saved:</h2>

      <ul>
      <% form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reference_model %>
    <%= f.text_field :reference_model %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <div class="field">
    <%= f.label :open_at %>
    <%= f.date_select :open_at %>
  </div>

  <div class="field">
    <%= f.label :close_at %>
    <%= f.date_select :close_at %>
  </div>

  <div class="field">
    <%= f.label :max_attemps %>
    <%= f.number_field :max_attemps %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
